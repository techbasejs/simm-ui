import { Button } from "@simm/core";
import { Demo, ButtonDemoBasic, ButtonDemoDisabled, ButtonDemoSizes, ButtonDemoVariants, ButtonDemoLoading, ButtonDemoIcon, ButtonDemoColors } from "@simmx/demo";
import { CodeHighlighter } from "@simmx/code-highlighter";
 
# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage

# IconButton

Buttons allow users to perform actions and choose with a single tap.

## Usage
